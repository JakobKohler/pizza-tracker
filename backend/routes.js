const endpoints = {
    '/api/v1': 'API root',
    '/api/v1/pizzaStats': 'Get pizza data of all users',
    '/api/v1/pizzaStats/:user': 'Get pizza data for a specific user'
};

module.exports = endpoints;
